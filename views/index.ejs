<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
    <title>Mi Aplicación - Bienvenido</title>
    <link rel="stylesheet" href="stylesheets/theme.css">
    <link rel="stylesheet" href="stylesheets/style.css">
</head>
<div ></div>
<body>

    <header class="cabecera">
        <div class="logo">
          <h1 class="logo-estilo">Mi aplicacion</h1>
        </div>
        
        <nav class="navegacion">
            <ul>
                <li><a href="#inicio">Inicio</a></li>
                <li><a href="#servicios">Servicios</a></li>
                <li><a href="#contacto">Contacto</a></li>
            </ul>
        </nav>
    </header>

    <main class="zona-principal">
        
        <section class="hero-card">
            
            <div class="Texto">
                <h2 >¡Bienvenido!</h2>
                <p>Descubre todas las funcionalidades que tenemos preparadas para ti. Únete a nuestra comunidad y empieza a disfrutar de una experiencia única.</p>
                <p>Accede a tu cuenta o regístrate en segundos para comenzar tu aventura con nosotros.</p>
            </div>
            
            <div class="card-interiores">
                
                <div class="registrado" id="card-registrado">
                    <p class="registrado-texto">Ya estoy registrado</p>
                    <form class="slide-form" id="form-registrado" novalidate>
                        <label for="email-registrado"></label>
                        <input type="email" id="email-registrado" name="email" placeholder="danigalachosoler@gmail.com" required>

                        <label for="password-registrado"></label>
                        <input type="password" id="password-registrado" name="password" placeholder="••••••" required>

                        <div class="botones-form">
                            <button type="submit" class="btn-gradiente">Enviar</button>
                            <button type="button" class="btn-secundario" id="cancelar-registrado">Cancelar</button>
                        </div>
                    </form>
                </div>
                
                <div class="registrado">
                    <p>No estoy registrado aún</p>
                   
                </div>

            </div>
            
        </section>

    </main>

    <footer>
      <p>By Danielgalacho</p>
        </footer>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const cardRegistrado = document.getElementById('card-registrado');
        const cancelarBtn = document.getElementById('cancelar-registrado');
        const formRegistrado = document.getElementById('form-registrado');

        const abrirFormulario = () => {
            cardRegistrado.classList.add('activo');
        };

        const cerrarFormulario = () => {
            cardRegistrado.classList.remove('activo');
            formRegistrado.reset();
        };

        cardRegistrado.addEventListener('click', (event) => {
            if (cardRegistrado.classList.contains('activo')) {
                return;
            }
            abrirFormulario();
        });

        formRegistrado.addEventListener('click', (event) => event.stopPropagation());
        formRegistrado.addEventListener('submit', (event) => {
            event.preventDefault();
            event.stopPropagation();
            // Aquí podrías realizar la lógica de envío real.
        });

        cancelarBtn.addEventListener('click', (event) => {
            event.stopPropagation();
            cerrarFormulario();
        });
    });
</script>

</body>
</html>